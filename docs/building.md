# Сборка образа системы (HeadUnit OS)

Система сборки построена по модульному принципу. Скрипт-оркестратор последовательно запускает этапы (`stages`), конфигурируя образ на основе INI-файла.

## 📂 Структура

```text
builder/
├── build.sh                # Точка входа (Оркестратор внутри Docker)
├── headunit.conf           # Конфигурация (Размеры, Поьзователи, Сеть)
├── lib/                    # Библиотеки
└── stages/                 # Этапы сборки
    ├── 01_make_image.sh    # Создание .img
    ├── 04_sys_config.sh    # Настройка ОС
    └── ...
```

## 🚀 Управление сборкой (Build CLI)

Управление осуществляется через PowerShell-скрипт `build.ps1`, который является оберткой над Docker.

### Основные команды

**1. Сборка текущего состояния (Dev Mode)**
Собирает образ из текущих файлов на диске (включая незакоммиченные изменения).

```powershell
.\build.ps1
```

- Если есть изменения, версия получит суффикс `-dirty`.
- Образ будет называться: `headunit-<версия>-dev.img`.

**2. Сборка для релиза (User Mode)**
Создает "чистый" образ без SSH и с полноценными разделами.

```powershell
.\build.ps1 -Mode user
```

**3. Просмотр доступных версий**
Показывает список Git-тегов.

```powershell
.\build.ps1 -ListTags
```

**4. Сборка исторической версии (Time Machine)**
Безопасно переключает репозиторий на указанный тег, собирает образ и возвращается обратно.
**Требование:** Рабочая папка должна быть чистой (без незакоммиченных изменений).

```powershell
.\build.ps1 -Checkout v0.1.0 -Mode user
```

**5. Интерактивная отладка**
Вход в консоль контейнера.

```powershell
.\build.ps1 -Interactive
```

## 🏷 Версионирование

Система автоматически определяет версию на основе Git:

1. **Git Tag:** Если коммит помечен тегом (напр. `v1.0.0`), используется он.
2. **Git Hash:** Если тега нет, используется хеш + смещение (напр. `v1.0.0-5-g3f1a2b`).
3. **Dirty State:** Если есть локальные правки, добавляется `-dirty`.

Версия "запекается" внутрь образа в файлы:

- `/etc/headunit-release` (OS Info)
- `/opt/headunit/version.json` (Для приложений)
- `/etc/motd` (Приветствие консоли)

## ⚙️ Конфигурация (Headunit.conf)

Настройки находятся в `builder/headunit.conf`.

- `[common]`: Общие настройки.
- `[dev]`: Переопределения для разработки (маленькие разделы, SSH).
- `[user]`: Настройки для продакшена.
